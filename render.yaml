services:
  - name: review-diagnostic
    type: web
    runtime: python
    plan: free
    rootDir: ./webapp
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: SENDGRID_API_KEY
        sync: false
      - key: SENDGRID_FROM_ADDRESS
        sync: false
