services:
  - name: review-diagnostic
    type: web
    runtime: python
    plan: free
    rootDir: ./webapp
    buildCommand: pip install --index-url https://download.pytorch.org/whl/cpu torch && pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: RESEND_API_KEY
        sync: false
